import React from "react";
import ReactDOM from "react-dom/client";
import InsetFaceLandingPage from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <InsetFaceLandingPage />
  </React.StrictMode>,
);
